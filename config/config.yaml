base_url: "https://station-developer-staging.aevatar.ai/godgpt-client/api"
browser:
  name: "chrome"
  headless: true
  window_size: "1920x1080"
timeout:
  implicit: 10
  explicit: 20
  page_load: 30

browser:
  chrome:
    options:
      - "--headless"
      - "--no-sandbox"
      - "--disable-dev-shm-usage"

report:
  path: "reports"
  filename: "report.html" 