test:
  base_url: "http://example.com"
  timeout: 10
  headless: true

browser:
  chrome:
    options:
      - "--headless"
      - "--no-sandbox"
      - "--disable-dev-shm-usage"

report:
  path: "reports"
  filename: "report.html" 